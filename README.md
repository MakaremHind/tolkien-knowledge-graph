## Triplestore

We use Apache Jena Fuseki (TDB2) as RDF triplestore.

- Server URL: http://localhost:3030/
- Dataset name: `tolkien`
- SPARQL endpoint: http://localhost:3030/tolkien/sparql

The triplestore is used to store and query the generated Knowledge Graph.
